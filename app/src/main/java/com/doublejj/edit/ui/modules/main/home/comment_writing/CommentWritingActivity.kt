package com.doublejj.edit.ui.modules.main.home.comment_writing

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import androidx.lifecycle.ViewModelProvider
import com.doublejj.edit.R

class CommentWritingActivity : AppCompatActivity() {
    private val TAG: String = javaClass.simpleName.toString()
    /*private lateinit var binding: CommentWritingActivityBinding
    private lateinit var viewModel: CommentWritingViewModel

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        binding = setContentView(this, R.layout.activity_comment_writing)
        binding.lifecycleOwner = this

        viewModel = ViewModelProvider(this).get(CommentWritingViewModel::class.java)
        binding.commentWritingViewModel = viewModel
    }*/
}